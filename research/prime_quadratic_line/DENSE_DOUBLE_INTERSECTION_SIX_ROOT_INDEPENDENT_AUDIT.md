# Independent audit

PASS, 2026-09-19. Actual frozen proof checked independently.

The exact edge count follows from incidence Cauchy–Schwarz and the maximum pair intersection two. Anchoring a point gives distinct affine explanations with pair intersection at most one, yielding exactly R=(n−1)(T−2)/((T−1)²−(n−1)). The strict denominator hypothesis and T≥3 are sufficient. Using R_+=max(R−2,0) handles the finite cases R<2 without reversing inequalities.

For each graph edge the common-neighbor count is at least d_i+d_j−M, so three times the triangle count is at least Σd_i²−Mm. Cauchy–Schwarz yields m(4m−M²)/(3M). The number of triangles with a common coordinate is at most Σbinom(r_x,3)≤R_+ I/3≤R_+M(M−1)/3. Every other triangle has pairwise-disjoint two-element intersections. Thus the finite bound and the coefficient β(2β−1)/6 in the M≫√n asymptotic are correct. Substitution of the edge lower bound is legitimate in this regime because the quadratic triangle expression is increasing there.

The three quadratic differences have their indicated distinct two-root supports and sum to zero, giving the displayed determinant. Their three monic sum/product points are distinct, hence define one line. A zero discriminant would factor this line equation and force a shared root; thus β₀²−αγ≠0. The trace-zero matrix of the stated fractional-linear map squares to this scalar times identity. In odd characteristic it is nonidentity, has no pole at the six roots, and swaps each pair. All coefficients may be taken in Fp because the three monic root-pair vectors are native.

The result counts polynomial triangles, not distinct involutions or pencils. It does not force one global coefficient plane. The text accurately preserves these limitations.

Frozen source SHA256: `bfb13309c684dd69a176ade78095b520e8740f776e242e50f93d9604d7d1ab8a`.
