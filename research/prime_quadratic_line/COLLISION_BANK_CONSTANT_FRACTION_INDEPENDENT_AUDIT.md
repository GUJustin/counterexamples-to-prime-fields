# Two complete bipartite components: independent constructive audit

2026-09-18. **PASS.** Integration-ready proof: `collision_bank_constant_fraction.tex`, label `thm:collision-bank-constant-fraction`. This note records the changes from the earlier Sidon bank and the audit-sensitive points. No main manuscript edits.

1. **Prime selection is not circular.** Set t=floor(v_H/16), where v_H is the number of primes in [H/2,H], before choosing p. Define L=2t², M=floor(L/8), Q0=LHM and choose p≡1 mod4 in (4096Q0,8192Q0). A largest quartic class always contains at least 4t small primes. Also p>H^4 eventually. The fixed-progression prime number theorem supplies these primes; no uniform character equidistribution of the small primes is needed.

2. **Coherent signs matter and are supplied.** Choose a reference z in the common quartic class, square roots α_a and β_b that are themselves squares and satisfy α_a²=a/z, β_b²=b/z. Then θ_ab=α_a/β_b is square, θ_ab²=a/b, and the two rectangle diagonal products are exactly equal. Arbitrary independent root signs would not establish the exact core regularity. Reference z may be one of the selected primes; no extra class member is required.

3. **Multiplicity is exactly controlled.** Squaring a product equality and clearing denominators yields an integer equality, since both sides are at most H^4<p. Unique factorization with disjoint numerator/denominator prime sets leaves at most two pairings. The second pairing exists precisely for a rectangle within one K_(t,t). Distinct-edge pair products never have more than two owners; repeated-edge products cannot create an extra representation. Pair sums for distinct owners are different, or their parameter pairs would be identical.

4. **The core is exactly regular.** Give a unique-owner pair both roots; give the two rectangle pairs one root each. At a core coordinate only the assigned two bank words match. Each edge has exactly (t−1)² opposite partners, hence exact agreement A=2(L−1)−(t−1)²=3t²+2t−3. Total core size is LA/2=t²A. Any nonbank quadratic has at most L matches by the same two-owner incidence count. This remains valid though the same squared coordinate now has different received values at its two roots.

5. **The grid event and collision event coexist.** Expected retained count is (1/2−o(1))M²: the deletion loss divided by M² is O(L²/p)=O(1/H). Thus retained≥M²/4 has probability ≥1/3−o(1). For all raw interval labels, E C≤binom(L,2)(3HM)²/p. Markov bounds failure of C≤LHM/64 by 288/4096, strictly smaller than 1/3. Dependence between these events is irrelevant; probability subtraction gives a simultaneous realization.

6. **Rich-fiber and singleton constants survive.** With d=floor(M/(64H)), every bank has ≥HM/16 rich labels on the retained event. Removing both endpoints of every cross-bank raw-label collision leaves ≥LHM/32 labels. Removing λ=−c0 excludes the sole nonbank exception, the zero polynomial matching the entire grid. All other nonbank quadratics have ≤L+2M+3<A agreements after neutral padding. Collision removal is against all raw bank labels, so no second bank, even a non-rich one, can reach the threshold at a retained label.

7. **Padding has positive room.** A~3L/2, Ncore~3L²/4 and M²≤L²/64, whereas n=floor((A+d)²/2)+1~9L²/8. Therefore there is room for the grid and neutral coordinates. Choose neutral f=X³,g=0 outside bank/cubic intersections. The field has p≫n, so the extra ≤3L exclusions are harmless. Bank core agreement stays A and outsider neutral agreement is ≤3.

8. **Exact sources and CA.** Avoid all raw bank labels and zero when choosing c0; choose two finite far labels outside the shifted raw union and the exceptional zero label. Both source maxima are exactly A. For a nonzero explaining direction, at most two common coordinates lie in the zero-direction block and at most 2M lie in the grid, less than A. A zero explaining direction restricts to the core/neutral block with maximum A. Thus ordinary CA=A. Invertible endpoint change preserves it; normalized affine mixtures enumerate the same line, losing no rich labels.

9. **Final count and scales.** At least Q0/32−1>p/2^18−1≥p/2^19 labels have singleton lists eventually. The scales are n=Θ(H^4/(log H)^4), d=Θ(H/(log H)^2)=Θ(n^1/4/log n), p=Θ(H^5/(log H)^4)=Θ(n^5/4 log n). Both A and T are above first order eventually, since divided by sqrt n they tend to sqrt2 rather than sqrt(3/2); T²<2n exactly. The normalized source loss is Θ(n^−3/4/log n), smaller than the Θ(n^−1/2) capacity/first-order scales.

The proof establishes an asymptotic constant fraction with conservative constant 2^−19; it does not supply a finite onset. Successful witnesses have at least T matches, not necessarily exactly T. Dimension remains three; the alphabet and domain are prime-field but the domain is not prescribed. This is neither fixed-rate progress nor an n² exceptional-count lower bound. The earlier fully Sidon theorem remains a different count/gap tradeoff, so no blanket superiority statement is made.
