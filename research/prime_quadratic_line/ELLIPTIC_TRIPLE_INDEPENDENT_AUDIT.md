# Elliptic triple core: independent audit PASS

Audited `ELLIPTIC_TRIPLE_CORE.md` without changing the main manuscript.

For each nonzero R in cyclic prime-order H, there are ell−2 possible second points before excluding −R, −2R and −R/2. The exclusions are distinct and otherwise allowed for ell>3. The involution S↦−R−S pairs the remaining ell−5 choices without fixed points. Thus every bank point lies on (B−4)/2 retained lines, with B=ell−1. A nonsingular plane cubic has no line component, so each retained line meets exactly the stated three distinct bank points. The resulting 2 roots per line give N₀=B(B−4)/3 and A=B−4.

Avoiding finitely many points and line intersections by a rational projective chart is legitimate, even though the forbidden coordinates are algebraic: rational projective transformations are Zariski dense over the algebraic closure. The chosen infinity line gives distinct line directions; choosing the coordinate directions away from the retained lines makes their slopes finite and nonzero. Each core match with an outside quadratic contributes three roots among B nonzero quadratic differences, proving the unrestricted nonbank bound floor(2B/3).

All coordinates and evaluation square roots lie in a finite number field. A finite Galois closure, complete splitting, and finitely many bad-prime exclusions give infinitely many prime-field realizations. Characteristic two and all required denominators/differences must be excluded. This is not an all-large-primes or small-field statement.

Fresh greedy selection must exclude zero and all pairwise bank intersections, not just retained triple intersections. The degree-four prescribed-label equation is always nonzero. For B≥52 the stated bound 5B⁴ is more than sufficient. The nonbank total bound floor(2B/3)+4≤B−4, common-agreement bound five for a nonzero direction explanation, and zero-only infinity list all follow exactly as in the pair-core construction.

The formulas t=(B²−10B+30)/6 and n=((B−3)²+1)/2 are integers because B is even and is 0 or 4 modulo 6. With T=B−3, T²=2n−1. For T≥49, the low-rate first-order upper estimate is strictly less than

    (7/8)T+(3/4)sqrt(T)<T.

The two terms are bounded respectively using n=(T²+1)/2; the final inequality holds for T>36. The leading bad-challenge coefficient sqrt(2)/3 is indeed 4/3 times the pair-core coefficient. All assertions are within the stated fixed-degree, vanishing-rate, one-coordinate-gap scope. No novelty of the underlying elliptic collinearity construction is asserted by this audit.
