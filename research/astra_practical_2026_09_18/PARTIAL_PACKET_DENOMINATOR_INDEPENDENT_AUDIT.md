# Independent audit of the partial-packet denominator bound

2026-09-18. PASS within the stated exact-locator model.

The leading coefficient of the affine residual is affine in the challenge. Every normalized coefficient in the high band is therefore a fractional-linear function of that challenge. Since the locator has base-field roots and is monic, its coefficients lie in the base field. More than p distinct challenges force each such fractional-linear function to be constant. This establishes the full common band without a prescribed-head assumption.

For the cross-rotation argument, equal total locator degrees imply that partial-factor degrees differ by a multiple of B. Their leading rotation phases consequently agree even when whole-packet counts differ. The common band makes the cross-rotation numerator have degree at most e_i+e_j-m. If this is negative it vanishes identically. Its ratio belongs to F_p(X^B); valuation differences are constant on full packets. Removing all full packets from each partial factor makes the remaining partial occupancy identical.

For the arbitrary-denominator claim, the contraction of the principal divisibility condition to E[Y] is the nonzero ideal generated by tau. Coprimality of the fixed root locator R and the root-free denominator T is essential and follows from their domain assumptions. The kernel dimension bound is floor((J-1+d-c)/B)-deg(tau)+1, truncated at zero and by the ambient degree. Since deg(tau)>=ceil(d/B), the stated bound follows. The preimage of the two-dimensional pencil has dimension at most kernel+2; imposing monicity reduces this by one. Base-field linear independence remains extension-field linear independence by the minor criterion.

At the benchmark, J-1-A=-8711, so floor((J-1-A)/512)=-18, giving coefficient codimension at least 16. The incumbent has J-1-A=-8704, giving codimension at least 15. The exact verifier was rerun successfully.

Scope: the residual is assumed to be a nonzero scalar times the exact monic locator of A base-domain coordinates. Residuals with additional varying factors, including roots outside the evaluation domain, are not covered merely by this argument. Nor is coefficient codimension an upper bound on the number of root subsets in an exceptional coefficient fiber. Thus this does not prove that the target bank is impossible, and it does not supply a better.codes certificate.
