# Integrated Hermitian follow-ups: independent audit

2026-09-19. **PASS** for the actual fragment hashed below. No manuscript edits or new finite enumeration.

The fixed-rate padding corollary retains its crucial domain containment S⊂E and uses the independent degree-eight coefficient to force reciprocal agreement at every endpoint match. Individual/common agreement is exactly k. Every displayed Hermitian witness still has exactly D−1 roots on the padded domain, and all M labels remain interior. The Johnson slack and fixed-rate asymptotics are correct. The text appropriately does not claim singleton completeness after padding, and notes that the threshold remains below first order.

The zero-endpoint lemma correctly requires even e, m=p+e dividing p²−1, and L>e. The two trace-zero choices yield a native pole outside mu_m, the Möbius normalization is valid for every exterior beta, and the determinant argument proves cofactor coprimality. P has exactly D−e native roots and the divided difference has degree<k. The p=11 finite paragraph agrees with the saved certificate: modulus data, a=3+2v, b=8+9v, gamma=6, degree98 in dimension99, and exact106 matches. This receipt inspects that recorded certificate; it does not claim a new replay.

The second-endpoint lemma correctly uses the more general hypotheses p modulo five in {2,3} and e≡p modulo five. Then p³≡−p and an exterior fifth root t exists, so the actual displayed rational map evaluates to beta^(p³). Its G is split and squarefree, F is coprime to G, and the identity c*=−Λ(beta)/Λ(beta)^p makes the divided difference polynomial. Evenness and L>e are correctly absent from this lemma; they are unnecessary for c* alone.

The final proposition has the right quantifier order: for every epsilon, choose a fixed odd r≡3 modulo five with 1/(r−1)<epsilon. The residue 7r²−5r−1 is coprime to 10r(r−1), so infinitely many primes occur in that one progression. Both competitor lemmas apply to the SAME primes and EVERY exterior beta. The previous theorem supplies strict farness, and both losses are at most e−2, with ratio tending to 1/(r−1). No unproved uniform bound on primes in progressions or infinite prime-quadratic assertion is used.

The concluding scope is accurate: this limits the two specified quartic endpoints, not the optimal pair on the line, and does not contradict the degree-eight padded sources with exact agreement k. The distinction between exact witness agreement and nearest agreement is preserved: exact saturation is asserted only where the earlier upper bound matches the p=11 or prime-quadratic construction.

Source SHA256: `fedb63cb6f69fe61466aec1b7ab6434984e7e1a24b0616ffe1b1710b3859bab8`.
