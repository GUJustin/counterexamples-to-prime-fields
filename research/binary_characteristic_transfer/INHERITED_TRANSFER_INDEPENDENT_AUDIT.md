# Independent audit of inherited puncturing and shortening

September 18, 2026. **PASS** for `INHERITED_FIXED_RATE_TRANSFER_LIMIT.md`, within its explicit inherited-witness and operation-order scope.

1. **Pair bound.** A rich three-plane contributes at least A(A-p+1) ordered linearly independent retained pairs. For a fixed two-plane U, distinct three-planes containing U intersect exactly in U, so their retained points outside U are disjoint. With s=|S intersect (U minus zero)|≤C, their number is at most (n-s)/(A-s)≤(n-C)/(A-C); the latter ratio increases with s. At most n(n-1) ordered pairs exist globally. This proves Eq. (1) exactly.

2. **Triple bound.** A line is contained in a two-plane. The simultaneous old-source explanations imply at most C retained support points on either a forbidden line or a forbidden two-plane. Choosing three independent points successively gives at least A(A-C)² triples per rich three-plane. Each independent triple determines its plane, proving Eq. (2).

3. **Scalar-witness bound.** In h_i=u_i Q with nonzero Q and degree≤D, each candidate matches at least A-D coordinates with Q(x) nonzero. A private coordinate line contributes only one incidence, for a total at most n. Every public nonvertical line has two distinct-label parameter points and therefore defines simultaneous degree≤D explanations f=c0 Q and g=c1 Q at all coordinates with that line constraint, as well as universal constraints. Their combined coordinate count is at most C. Each public incidence has at least A-C matching second coordinates with independent constraints; two independent coordinate constraints identify at most one parameter point. This gives (M(A-D)-n)(A-C)≤n(n-1). Parallel distinct constraints cannot both pass through the same candidate point. Vertical constraints are allowed as second coordinates, and universal constraints were charged to C.

If Q=0, a nonuniversal coordinate can match at most one distinct label. Universal coordinates number at most C. The function (n-s)/(A-s) is increasing, yielding M(A-C)≤n-C. Thus the affine-in-label special case is also sound.

4. **Shortening dichotomy.** Canonical h_i have shape u_i X^(p-1)+v_i. A common matched point x0 is nonzero. If all shortened points are on x0 F_p^*, the two original sources are constant there, and subtracting these constant explanations makes all witnesses scalar multiples of X^(p-1)-x0^(p-1). Division by the common locator and subsequent common-zero lengthening preserve a fixed scalar factor. Alternative valid explanations differ by affine-in-label polynomials divisible by that locator, with the required reduced degree bound. If a shortened point is off this line, its additional equation forces u_i affine in z_i; the complete witness family is then affine in label. With two labels, its two coefficient polynomials are valid simultaneous explanations on the shortening set. The remaining one-label case is trivial.

5. **Scope.** At final A-D≥delta n, the scalar lemma gives M≤1/delta+(n-1)/(delta(A-C)); fixed fractional common-agreement gap bounds M by a constant. Arbitrary puncturing alone has the corresponding conclusion from Eq. (1). Common-zero lengthening after puncturing can be stripped before applying that bound: n0=n-z, A0=A-z, C0≤C-z, and A0-(p-1)=A-D≥delta n. This retains the claimed O(n/(A-C)) scale. These arguments cover the stated inherited families, not fresh witnesses created by modifying coordinates or a changed source family.

The proof is compatible with the strengthened singleton classification at all thresholds >p²: it relies only on the designated canonical witnesses and old simultaneous two-plane explanations. Neither classification nor this incidence argument supplies a universal fixed-rate obstruction for arbitrary constructions. No computation or manuscript edit was needed.
