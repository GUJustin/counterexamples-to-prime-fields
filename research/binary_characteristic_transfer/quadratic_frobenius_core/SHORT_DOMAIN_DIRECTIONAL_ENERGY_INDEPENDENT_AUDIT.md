# Independent audit: directional-energy target and conditional incidence barrier

2026-09-18. **PASS** for the integrated note at the hash below.

The appended implication is valid with K and c fixed independently of m,p. Write q for the number of r-rich lines. If q=0 there is nothing to prove. Since r≥c√m→∞ and m/(pr)≤√m/(cp)→0 under m=o(p²), eventually Kq+Kmq/p≤rq/2. Thus

    rq/2 ≤ K m^(2/3) q^(2/3) + K m.

At least one right-hand summand is ≥rq/4. The first alternative gives q≤(4K)^3 m²/r³; the second gives q≤4Km/r. Both are O_(K,c)(√m). Parallel rich lines are disjoint, so R_r(a)≤m/r and

    sum_a R_r(a)² ≤ (m/r) sum_a R_r(a) = (m/r)q = O_(K,c)(m).

No point-line incidence theorem over finite fields is being asserted. The conclusion is conditional on the displayed estimate for this particular rich-line family. The density assumption is essential for absorbing the mq/p term; this does not exclude the dense higher-power construction.

The integrated construction ledger remains consistent: ordered canonical pairs give distinct nonzero labels, zero subtraction is at most floor(m/r), and curve control is an additional hypothesis rather than a consequence of ordinary line richness. The numerical sufficient target implies exact source/common agreement 5L, threshold asymptotic to 6.6√m, and superlinear singleton labels only if an actual point set with the required energy and caps is supplied. The appendix says such a short-domain set would violate the specified uniform incidence estimate, not that the set is impossible by a known theorem.

No edits to the target note or manuscript and no computations/scans were needed.

Source SHA256: `ec270eb2d88ce704bb1f987a8346d90757a10e6a9e58b6fd309fe1bb31eba5af`.
