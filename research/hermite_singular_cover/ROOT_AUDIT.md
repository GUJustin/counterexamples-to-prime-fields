# Independent root review — September 17, 2026

PROOF.md passes independent mathematical review. This is a conditional
actual-solution theorem, not a universal coding statement or a formal proof.

The selected core supports must consist of singular agreements. Removing
labels with L regular matches first leaves at least kappa+1 such matches
inside the two cores, so the derivative prescription is applicable.
With at most m=3(N-b) missing core coordinates, their surviving weighted
intersection is at least max(0,N-m)+max(0,s-m). This equals the stated
two-piece expression. Its being greater than D is equivalent to crossing
the minimum of the two displayed real thresholds, with the stated floor
and N truncation. A triple affine dependence vanishes to order at least
two at every surviving Hermite coordinate and to order at least one at
the others, hence vanishes identically. This multiplicity argument is
valid over the stated characteristic range. The final affine-pencil
count uses original full agreement supports, including extra coordinates.

For the quadratic derivative specialization, the three degeneracy cases
are exhaustive. When a(x) is nonzero, singularity fixes the derivative
and forces the discriminant to vanish; its degree in z is at most two.
When a(x)=0, singularity forces ell_x=0. If ell_x is identically zero,
the actual equation still imposes m_x=0; this last step is essential
for obtaining a finite incidence bound outside the ordinary core.
The pure Hermite derivative data are affine in z because the original
coefficients are challenge independent. The stronger characteristic
condition excludes 2 as required for division by 2a(x).

The field-counting bound counts coordinate-label pairs, not polynomial
candidates, and is therefore uniform over arbitrarily many solutions.
The exceptional-label estimate 2n/(e+1) and L>=lambda*n/2 are correct
with e=floor(lambda*n/2), including small n. The formula for C follows
from B=2,H=0 in the established nonsingular theorem.

No unrestricted bound follows when the actual persistent cores exceed
the stated weighted threshold. The integer enumeration checks rounding
and the weighted intersection ledger, not the differential geometry.
The logical proof review above is the evidence for the latter.
