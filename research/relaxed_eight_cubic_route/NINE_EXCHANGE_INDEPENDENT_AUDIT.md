# Independent audit: nine quartics on eighteen points

**PASS, with an at-least-eight agreement claim only.** The independent stdlib verifier `verify_nine_exchange.py` reconstructs the structured incidence Jacobian from the original eight-cubic seed, rather than loading the saved Jacobian. The receipt is `nine_exchange.verified.json`.

Use the seventy variables consisting of eight cubic coefficient vectors, sixteen domain coordinates, sixteen received values, the pole b, and five numerator coefficients. The equations are the fifty-six old selected incidences P_i(x_j)=w_j and eight numerator incidences N(x_j)=(x_j-b)w_j. At the displayed point b=0 and N has ascending coefficients (2,15,8,5,13). Delete old row 0, namely P_0 at x=3, and numerator row 56, namely N at x=1. The remaining sixty-two equations have full row rank. An exact integer Bareiss determinant of the original selected 62 by 62 matrix is 8 modulo 17; its row and column indices are saved in the receipt.

The factorization, checked by independent coefficient multiplication, is

    N(X)-X P_0(X)=11(X-5)(X-6)(X^2+10).

Seven is a nonsquare modulo seventeen. In F_289=F_17(theta), theta^2=7, the fresh root theta is outside all old coordinates and the pole. The derivative of the displayed difference at theta is 6+15 theta, which is nonzero. The numerator has N(0)=2, so the rational function is proper at its pole.

For the resulting quartics use (X-b)P_i(X), i=0,...,7, and N(X). At each old coordinate use received value (x_j-b)w_j. Append the pole b with received value zero, and append the fresh root theta with received value N(theta). The first eight quartics all match at the pole. The first quartic and the numerator both match at theta. Thus the deleted old incidence of P_0 is replaced by theta, and the deleted numerator incidence is replaced by theta. Every one of the nine quartics has eight prescribed matches. Independent tuple arithmetic verifies eighteen distinct domain points and nine distinct quartics. The actual residue-field match counts are (9,8,8,8,8,8,8,8,9), so this certificate does not establish a nearest list or its completeness.

## Characteristic-zero and prime-field lift

Fix the eight nonpivot variables to their integral representatives. The unit sixty-two by sixty-two minor gives a square nonsingular system, and multivariate Hensel lifts it over Z_17. Its coordinates are algebraic over Q: the square Jacobian is invertible in characteristic zero, so the resulting local solution is zero-dimensional over the field of definition. Equivalently the differential module of the field generated by the coordinates over Q vanishes. Consequently this produces a number-field point embedded in Q_17.

Now adjoin the unramified quadratic extension with residue field F_289. The equation

    N(theta)-(theta-b)P_0(theta)=0

has a simple root at the specified residue point, so Hensel gives an algebraic root lifting it. Set the new received value to N(theta). This is an etale root extension of the already smooth base construction; one need not assert full rank for the unconstrained seventy-two-incidence quartic parameterization. All distinctions, nonzero leading coefficients, N(b) nonzero, and separations from the pole persist because their residue values are nonzero.

Take a finite Galois number field containing this construction. Outside finitely many primes all coefficients are integral and all listed nonzero guards remain nonzero. Arbitrarily large rational primes split completely in that field, yielding eighteen distinct prime-field nodes and nine distinct degree-four polynomials with at least eight agreements each. This is an n=18,k=5,A=8,L>=9 list construction; it is not a complete-nearest-list assertion or an unbounded amplification theorem.

## Subsequent completeness upgrade

The initial at-least-nine scope above has now been strengthened by an independent exhaustive decoder and defect audit, saved in `verify_nine_complete.py` and `nine_complete.verified.json`. Newton divided differences reproduce 8,568 five-point subsets, 5,628 distinct interpolants, and exactly nine residue quartics with at least eight agreements. The two saved left-null vectors were checked against the independently reconstructed Jacobian and the directly evaluated integral equation defects. They force the deleted residuals divided by 17 to equal 13 and 2 modulo 17. Thus the two extra residue agreements fail in every unramified Hensel lift of the retained system, and all nine lifted quartics have exactly eight matches.

For an arbitrary lifted competitor with at least eight matches, integral five-point interpolation makes its reduction a known residue quartic, whose full residue support has size at most nine. Its support overlaps the known lift's eight selected matches in at least seven nodes. This forces equality of the degree-four polynomials and proves a COMPLETE nine-element nearest list with maximum agreement eight in characteristic zero and at all sufficiently good split primes. The standalone manuscript fragment `nine_quartic_exchange.tex` includes the proof and the degree-twelve pullback with parameters n=216,k=49,A=96 and at least nine codewords. Completeness of that pullback is not claimed.
