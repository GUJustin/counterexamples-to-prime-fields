# Replay of the actual current lower-track interpolation catalog

September17,2026. Live GitHub main was checked and equals
`cdb451f13fdc6c84f5fe363e77ee13a89bd30974`; the official site still shows
68.11--116.13. This directory examines the current68.11 moving-fiber
certificate, not the weaker single-explainer formulas in ePrint2026/2056.

## Current result: complete numerical reconstruction still fails

The target181275 pipeline has now been rebuilt with repaired primary,
auxiliary, scalar-list, carrier, and derivative-chain arithmetic. It misses
the corrected MCA budget by **7.65%**, with2841of5238contexts failing.
This is not a better.codes improvement. The baseline control reproduces
or improves all4970x8 incumbent singleton allowances, so this deficit is
not a failure to reproduce the old numerical inputs.

See [FULL_TARGET_RECONSTRUCTION.md](FULL_TARGET_RECONSTRUCTION.md) for
parameters, exact numbers, limitations and reproduction commands. The
immediate target is the singleton(r12,v43,z3206), which propagates through
prefix ancestors(13,43) and(17,45). Separate agents are improving auxiliary
source geometry and seeking an earlier phase-source activation there.
The unified input manifest restores101 checksum-verified pinned inputs.

The first integer score improvement really requires agreement181275;
[the exact score-cell audit](SCORE_CELL_FEASIBILITY.md) rules out the
intervening cells for6812centibits. Earlier sub-percent deficits used
frozen incumbent allowances and are retained only as historical conditional
diagnostics.

## Initial source audit

An independent Python implementation exactly reproduces all27 stored
coefficient counts and local-rank bounds from the current Lean source
catalog. All27 strict dimension inequalities pass at181284 agreements.
At the next-centibit target181275, every unchanged catalog source fails
that sufficient dimension inequality. Increasing only the total cap L
by25--99 repairs these individual interpolation gates, at a cost to the
subsequent geometric bounds that has not yet been recertified.

A bounded1080-case local search changes multiplicity by at most2 and
allows B,s,U each to decrease by at most1, holding L,k,n0 fixed.
Only source1 has replacements passing the target interpolation gate:

| Parameters(m,B,s,U,L,k,n0) | Surplus at A181275 |
|---|---:|
|(133,56,24,180,2819,6,8)|69790984828|
|(133,56,25,180,2819,6,8)|142336033852|

The existing source1 is(134,56,25,180,2819,6,8). These are genuine
dimension-feasible replacements under the same source theorem's
arithmetic hypotheses, but they do not replace the remaining26 sources
or prove a complete68.12 certificate. Changing agreement also changes
other counts, so preserving caps does not by itself preserve the final
soundness bound.

## Exact arithmetic and scope

The implementation counts the weighted global monomials directly from
their integer budgets and evaluates the local source-minus-kernel count
using a closed clipped-rectangle sum. Every original output agrees with
the pinned kernel-checked integer statements. The code checks the cap
conditions needed to use that closed rank formula. The replay completes
in0.56seconds; the bounded search in5.28seconds, both under384MiB/60sec.

The current proof uses27 sources arranged into16 triples. Its full
receipt covers35 derivative-index rows with r+v<=159, together with
phase, threshold, ledger, prefix and packing inequalities. Those receipt
inequalities have not been independently replayed here. The first
identified active obstruction is interpolation-source existence, not a
claim that it is the final or unique bottleneck. A failure of the
sufficient dimension inequality does not prove that the actual global
interpolation kernel is zero.

The exported radius is331206655/1073741824, corresponding to effective
agreement181283+1/4096 and hence integer agreement threshold181284.
For68.12 the analogous candidate effective agreement181274+1/4096
would require threshold181275. This is a numerical target only; no
new radius, full case cover, Lean proof or leaderboard gain is claimed.

## Subsequent downstream replay

The4970 stored component rows now pass224590 phase-endpoint checks,
121966 final-ledger endpoint checks, and99050 prefix-monotonicity checks.
The worst stored ledger upper bound is274693043573515013, leaving
287676976235792 (about0.1046%) of the274980720549750805 budget. It
occurs at(r,v,z)=(35,124,9090), using phase05 through witness6.
Its parent-prefix contribution266528645520722343 dominates the direct
phase charge1902187458836272.

Exact piecewise-affine optimization finds that **all49700 stored prefix
values are already minimal**, conditional on the stored base functions,
phase potentials, thresholds, and required monotonicity. An independent
power-band replay verifies all49220 active phase thresholds;480 are
inactive. None of the active thresholds can move one coordinate earlier
under the same sources. These statements concern the existing receipt,
not all alternative source choices.

All10 primary/phase kernel coefficient and rank counts also match the
pinned source exactly. At target A181275, primary A, B, and TCap lose
positive dimension surplus. The seven phase kernels remain positive
but lose roughly2.3--3.16% of their nullity; their old thresholds cannot
simply be reused. These primary and phase sources are additional to
the27 moving-fiber auxiliary sources. Repairing only those27 is not a
complete improvement strategy.

Upstream packing/base-source validation and target-parameter propagation
remain incomplete. Consequently the checks above must not be described
as an independent full replay of the entire68.11 proof or as a68.12
certificate. Target phase-threshold calculations are recorded separately
in bounded r-blocks as they complete.

`primary_sources.json` gives hashes and pinned URLs. The two multi-megabyte
foundation files are cached only under ignored
`tmp/current-lower-primary-cache/`; the smaller inspected source files
are included here for reproducibility. `replay_sources.py` only needs
the four source-catalog files. `source_replay.json` and
`dominating_source_search.json` record every result and the exact search
neighborhood. No repository or external submission was made.

For all replay inputs, run `fetch_primary_sources.py`. Its unified
`source_inputs.json` records64 pinned Lean files, exact SHA256 hashes,
URLs and restoration paths. The fetcher verifies existing files and
checksum-verifies any missing public-source downloads. The earlier
`primary_sources.json` and `cache_manifest.json` are partial inventories;
the unified manifest is authoritative for reproduction.
